{
  "name": "@mock-me/empty",
  "packageManager": "npm@10.2.4",
  "scripts": {
    "build": "turbo run build",
    "deploy": "turbo run deploy",
    "start": "turbo run start"
  },
  "workspaces": [
    "lib/*",
    "packages/*"
  ],
  "dependencies": {
    "turbo": "^2.0.3"
  }
}
